# JS Dojo 사용 설명서

학습자를 위한 실사용 안내서다. 무엇을 어떻게 치는지, 막혔을 때 어떻게 빠져나오는지를 담았다.
프로젝트 소개와 챕터 작성법은 [README.md](README.md)에 있다.

---

## 1. 준비

필요한 것은 두 가지뿐이다.

| 도구 | 버전 | 확인 명령 |
|---|---|---|
| Node.js | 22 이상 | `node -v` |
| git | 아무 최신 버전 | `git --version` |

Docker도, 계정도, 브라우저 확장도 필요 없다.

```bash
git clone https://github.com/ssj37v/jsdojo.git
cd jsdojo
npm install
```

`npm install`은 도장 엔진이 쓰는 두 개(`yaml`, `ajv`)만 받는다. 몇 초면 끝난다.
학습자가 `workspace/`에서 쓰는 코드는 원칙적으로 의존성 없이 Node 표준 기능만으로 돌아간다.

---

## 2. 시작하기

```bash
npm run dojo
```

1장부터 시작된다. 중간에 나가도 진도는 저장되며, 다시 실행하면 이어서 진행된다.

> **`--` 를 잊지 않는다.** 옵션을 줄 때는 npm에게가 아니라 도장에게 전달해야 하므로
> 반드시 `--` 를 사이에 넣는다.
>
> ```bash
> npm run dojo -- --status      ✔ 도장에게 전달된다
> npm run dojo --status          ✘ npm이 먹어 버린다
> ```
>
> 번거로우면 `node dojo/cli.mjs --status` 처럼 직접 실행해도 똑같다.

---

## 3. 도장 명령 전체

```bash
npm run dojo                          # 이어서 학습
npm run dojo -- --help                # 도움말

# 진도 보기
npm run dojo -- --status              # 전체 진도와 파일 위치
npm run dojo -- --outline             # 전 챕터 스텝 목차
npm run dojo -- --outline ch01        # 한 챕터만
npm run dojo -- --show ch01-s02       # 지난 스텝 다시 읽기

# 특정 챕터로
npm run dojo -- --chapter ch01

# 저장하고 되돌리기
npm run dojo -- --saves               # 저장된 지점 목록
npm run dojo -- --save 리팩터링직전     # 지금 상태를 이름 붙여 저장
npm run dojo -- --load 리팩터링직전     # 그 지점으로 되돌아가기

# 초기화
npm run dojo -- --reset               # 진도만 초기화 (workspace 코드는 그대로)
npm run dojo -- --restart             # workspace까지 지우고 처음부터 (지우기 전 자동 저장)
```

`--show` 는 **이미 통과한 스텝만** 보여준다. 아직 풀지 않은 스텝의 정답과 힌트는 열리지 않는다.

---

## 4. 학습 중 쓰는 키

스텝을 진행하는 동안 아래 한 글자를 치고 Enter를 누른다.

| 키 | 하는 일 |
|---|---|
| Enter | 검사한다. 실제 파일과 실행 결과로 판정한다 |
| `h` | 힌트를 연다. 처음에는 잠겨 있고, 잠깐 지나거나 한 번 틀리면 열린다 |
| `b` | 지난 스텝을 다시 읽는다. 코드도 진도도 건드리지 않고 제자리로 돌아온다 |
| `w` | 지금 상태를 저장한다 (진도 + workspace 코드) |
| `s` | 이 스텝을 건너뛴다 |
| `q` | 종료한다. 여기까지 저장되고 다음에 이어서 한다 |

힌트가 바로 열리지 않는 것은 의도한 설계다. 답을 먼저 보면 기억에 남지 않는다.
15초쯤 생각하거나 한 번 부딪혀 보면 열린다.

---

## 5. 검사는 어떻게 이루어지나

"했다"고 말하는 것으로는 통과되지 않는다. 도장은 세 가지만 믿는다.

| 검사 | 무엇을 보나 | 예 |
|---|---|---|
| 파일 | 실제 파일 내용 | `hello.mjs` 안에 `const` 선언이 있는가 |
| 실행 | 실제 종료 코드와 출력 | `node hello.mjs` 가 기대한 값을 찍는가 |
| git | 실제 저장소 상태 | 커밋이 남았는가, 브랜치가 맞는가 |

그래서 정답 문자열만 베껴 넣고 실행이 깨져 있으면 통과되지 않는다.
반대로 코드를 자기 방식으로 다르게 써도 동작이 맞으면 통과된다.

자주 쓰게 될 명령은 이 셋이다.

```bash
node hello.mjs        # 파일 하나를 실행한다
node --test           # 테스트 파일을 찾아 돌린다
node --watch app.mjs  # 파일을 고칠 때마다 자동으로 다시 실행한다
```

---

## 6. 막혔을 때

**검사가 계속 실패한다**
`h` 로 힌트를 세 단계까지 연다. 마지막 힌트는 사실상 답이다. 그래도 안 되면 `s` 로 넘기고
나중에 `--show` 로 다시 본다. 붙잡혀서 그만두는 것이 제일 나쁘다.

**코드를 망가뜨렸다**
`npm run dojo -- --saves` 로 저장 지점을 보고 `--load` 로 되돌린다.
스텝을 통과할 때마다 자동 체크포인트가 남으므로 되돌아갈 자리는 거의 항상 있다.

**전부 갈아엎고 다시 하고 싶다**
`npm run dojo -- --restart` 를 쓴다. `workspace/` 와 진도를 함께 지운다.
지우기 전에 자동으로 저장되므로 마음이 바뀌면 `--load` 로 되살릴 수 있다.

> 지우다 실패하면 편집기나 실행 중인 `node` 프로세스가 파일을 쥐고 있는 경우다.
> 그것들을 닫고 다시 실행한다.

**진도만 밀고 코드는 남기고 싶다**
`npm run dojo -- --reset` 은 진도 기록만 지운다. `workspace/` 의 코드와 git 이력은 그대로 둔다.

---

## 7. 커리큘럼

| 챕터 | 제목 | 스텝 |
|---|---|---|
| ch01 | 코드를 실행시키다 | 3 |

현재 **3스텝**이 준비되어 있다. 기초 문법에서 시작해 비동기, 모듈, 객체지향을 거쳐
디자인 패턴까지 이어지도록 챕터를 늘려 가는 중이다.

---

## 8. 내 코드는 어디에 있나

| 위치 | 내용 |
|---|---|
| `workspace/` | 네가 쓴 코드 전부. 네 소유다 |
| `.dojo/progress.json` | 진도 기록 |
| `.dojo/saves/` | 저장 지점들 |

셋 다 git에 올라가지 않는다. 도장 엔진이 네 코드를 커밋하거나 지우는 일은
`--restart` 처럼 네가 직접 요청했을 때, 그것도 확인을 받은 뒤에만 일어난다.
